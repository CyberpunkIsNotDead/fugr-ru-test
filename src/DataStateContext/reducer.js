// import { START_FETCHING, FETCH_ENTRIES } from './actionTypes'

// export const reducer = (dataState, action) => {
//   switch(action) {
//     case START_FETCHING:
//       return {...dataState, loading: true};
//     case FETCH_ENTRIES:
//       const dataHandler = (dataState, {data}) => ({
//         ...dataState, data: data, loading: false
//       });
//       return dataHandler();
//     default:
//       return dataState;     
//   };
// };
